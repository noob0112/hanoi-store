export enum ITEM_SORT_BY_ENUM {
  createdAt = 'createdAt',
  historicalSold = 'historicalSold',
  price = 'price',
}

export enum ITEM_ORDER_BY_ENUM {
  asc = 1,
  desc = -1,
}

export enum ITEM_OPTIONS_ENUM {
  LIMIT = 32,
}
